<%= form_for(@auto_year) do |f| %>
  <% if @auto_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auto_year.errors.count, "error") %> prohibited this auto_year from being saved:</h2>

      <ul>
      <% @auto_year.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
