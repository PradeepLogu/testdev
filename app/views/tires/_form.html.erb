<%= form_for(@tire) do |f| %>
  <% if @tire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tire.errors.count, "error") %> prohibited this tire from being saved:</h2>

      <ul>
      <% @tire.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
