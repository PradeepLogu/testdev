<h1>Listing auto_options</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @auto_options.each do |auto_option| %>
  <tr>
    <td><%= link_to 'Show', auto_option %></td>
    <td><%= link_to 'Edit', edit_auto_option_path(auto_option) %></td>
    <td><%= link_to 'Destroy', auto_option, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Auto option', new_auto_option_path %>
